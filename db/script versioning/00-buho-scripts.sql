-- NOTE: By default buho support has the id 21.

-- Add device to buho support account.
-- INSERT user_device (user_id, device_id) VALUES (21, <deviceId>);
-- INSERT user_device (user_id, device_id) VALUES (21, 117);

-- Delete device to buho support account.
-- DELETE FROM user_device WHERE user_id = 21 AND device_id = <deviceId>;

-- Find device by name
-- SELECT * FROM device WHERE name = ''

-- Find device by deviceId
-- SELECT * FROM device WHERE device_id = ;


